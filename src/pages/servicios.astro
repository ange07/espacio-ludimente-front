---
import MainLayout from '../layouts/MainLayout.astro';
import Button from '../components/Button.astro';
import PageHero from '../components/PageHero.astro';
import CTASection from '../components/CTASection.astro';
import { services } from '../data/services';
---

<MainLayout title="Servicios - Espacio Ludimente" description="Terapia infantil, evaluaciones psicopedagógicas y talleres para padres en Mérida.">
  <PageHero 
    title="Nuestros <em>Servicios</em>" 
    subtitle="Herramientas específicas para cada necesidad familiar"
    variant="centered"
  />

  <section class="services-list container">
    {services.map((service) => (
      <article class="service-detail card-hover">
        <div class="service-header">
          <div class="icon-wrapper">{service.icon === 'users' ? '👥' : service.icon}</div>
          <h2>{service.title}</h2>
        </div>
        
        <div class="service-body">
          <div class="section-block">
            <h3>¿Para quién es?</h3>
            <p>{service.target}</p>
          </div>

          <div class="section-block">
            <h3>¿Qué trabajaremos?</h3>
            <ul class="check-list">
              {service.focus.map(item => <li>{item}</li>)}
            </ul>
          </div>

          <div class="section-block">
            <h3>¿Cómo lo hacemos?</h3>
            <p>{service.method}</p>
          </div>

          <div class="service-footer">
            <p class="price"><strong>Inversión:</strong> {service.price}</p>
            <Button href="/reservar" variant="primary" size="md">
              Reservar cita
            </Button>
          </div>
        </div>
      </article>
    ))}
  </section>

  <CTASection 
    title="¿Tienes dudas sobre qué servicio necesitas?"
    description="Escríbenos y te orientamos sin compromiso."
    primaryCTA={{ text: "Contáctanos", href: "/contacto", variant: "secondary" }}
    secondaryCTA={{ text: "WhatsApp", href: "https://wa.me/529992590623", variant: "outline" }}
  />
</MainLayout>

<style>
  .services-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xl);
    padding-bottom: var(--spacing-xl);
  }

  .service-detail {
    background: white;
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-outline);
    padding: var(--spacing-lg);
    box-shadow: var(--shadow-sm);
    /* transition handled by .card-hover */
  }

  .service-header {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-md);
    border-bottom: 2px solid var(--color-surface);
    padding-bottom: var(--spacing-md);
  }

  .icon-wrapper {
    font-size: 3rem;
    background: var(--color-primary-container);
    width: 80px;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
  }

  .service-detail h2 {
    margin: 0;
    font-size: var(--text-headline-medium);
  }

  .service-body {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-lg);
  }

  .section-block h3 {
    font-size: 1.1rem;
    color: var(--color-primary);
    margin-bottom: var(--spacing-xs);
  }

  .check-list {
    list-style: none;
    padding: 0;
  }

  .check-list li {
    position: relative;
    padding-left: 24px;
    margin-bottom: 8px;
  }

  .check-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-secondary);
    font-weight: bold;
  }

  .service-footer {
    grid-column: 1 / -1;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: var(--color-surface);
    padding: var(--spacing-md);
    border-radius: var(--radius-md);
    margin-top: var(--spacing-md);
    flex-wrap: wrap;
    gap: var(--spacing-md);
  }

  .price {
    font-size: 1.1rem;
    margin: 0;
  }

  @media (max-width: 768px) {
    .service-header {
      flex-direction: column;
      text-align: center;
    }
    
    .service-footer {
      flex-direction: column;
      text-align: center;
    }
  }
</style>
